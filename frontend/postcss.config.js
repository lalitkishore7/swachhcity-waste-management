export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {}, // Autoprefixer is actually included in @tailwindcss/postcss usually but keeping it doesn't hurt or might be explicit
  },
}
